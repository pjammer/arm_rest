#Eventually this will be where you setup (at the very least) "migration "styled views.  See a view needs:
# 1, to be created, and 2, to be "ran".
# We are using the ArmRest::Server#find method to look up a view.
class Views < ArmRest::Server
  def initialize(args)
    
  end
  
  
end
